-- Vector sum (list of Int) with only final print super

sumVec xs ys = case xs of
  [] -> []
  (x:xt) -> case ys of
    [] -> []
    (y:yt) -> (x + y) : sumVec xt yt

main = printl (sumVec [1,2,3,4] [10,20,30,40])
